---
import type { CollectionEntry } from 'astro:content';
import BlogCardImageHorizontal from './BlogCardImageHorizontal.astro';

interface Props {
  posts: CollectionEntry<'articles'>[];
}

const { posts } = Astro.props;

---

<ul class='grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-4'>
  {
    posts.map((post) => {
      return (
        <li>
          <BlogCardImageHorizontal post={post} classList='bg-light-background_canon_card text-light-text dark:text-dark-text dark:bg-dark-background_canon_card'/>
        </li>
      );
    })
  }
</ul>
