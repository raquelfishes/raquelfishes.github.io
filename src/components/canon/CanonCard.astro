---
import {Icon} from 'astro-icon/components'
import Card from '@components/card/RaquelCard.astro'
import { Button } from '@eliancodes/brutal-ui';
import { getIcon, getColor } from '../../js/canon.js';

const { canonItem } = Astro.props; // Destructure the single `canonItem` passed from `CanonList`
---

<style>
div.canon-card-header {
    background-color: var(--heading-color);
    border-radius: 0.1rem;
    border: 1px solid grey;
    text-align: center;
    text-transform: capitalize;
}
.icon-inline{
    vertical-align: middle;
    display: inline-block;
    margin-right: 0.5rem;
}

blockquote::before {
    content: "\"";
    display: inline-block;
    font-size: 4em;
    line-height: 0.01;
    opacity: 0.5;
    vertical-align: -1rem;
}

p.blockquote {
    display: inline;
}

.canon-card {
    min-height:200px;
    flex-grow: 1;
}

.flex-container {
    display: flex;
    flex-direction: column; /* Stack children vertically */
    height: 100%; /* Ensure the card takes full height */
  }

.canon-card-footer {
    background: grey;
    box-shadow: 0 -5px 20px rgba(0, 0, 0, 0.1); /* Adds a soft shadow above */
    border-radius: 8px 8px 8px 8px; /* Rounded corners for the footer */
    text-align: center;
    padding: 1rem 0;
}
</style>

<script src="../../js/canon.js"></script>

<Card classList='p-4 bg-light-background_canon_card dark:bg-dark-background_canon_card'>
<div class="flex-container">
    <div class="canon-card-header text-light-black dark:text-dark-black" style={`--heading-color: ${getColor(canonItem.type)}`}>
        <h2 class="sanchez mt-2 mb-2 text-lg md:text-xl lg:text-3xl">
            <Icon name={getIcon(canonItem.type)} class="icon-inline"/>{canonItem.type}
        </h2>
    </div>
    <div class="flex-grow canon-card ml-4 mr-4">
        <p class='mt-4 outfit text-2xl md:text-4xl lg:text-4xl'>
            {canonItem.title}
        </p>
        {
            canonItem.quote ? (
            <blockquote class="mt-2 opacity-70">
                <p class='text-xl md:text-3xl lg:text-2xl italic blockquote'>{canonItem.quote}</p>
            </blockquote>
            ):(<slot/>)
        }
        <p class='mt-2 outfit text-xl md:text-3xl lg:text-2xl'>{canonItem.text}</p>
    </div>
    {
        canonItem.link ? (
            <footer class="canon-card-footer mt-8">
                <Button target={'_self'} href={canonItem.link}>
                Link &rarr;
            </Button>
        </footer>
        ) : (
        <slot/>
        )
    }
</div>
</Card>